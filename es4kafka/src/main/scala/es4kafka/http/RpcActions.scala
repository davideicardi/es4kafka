package es4kafka.http

object RpcActions {
  lazy val events: String = "events"
  lazy val commands: String = "commands"

  lazy val one: String = "one"
  lazy val all: String = "all"
  lazy val localParam: String = "_local"
}
